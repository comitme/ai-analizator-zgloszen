# --- Private working notes (strategy, sales, personal assessment) ---
NOTES.md

# --- Secrets ---
.env
*.key

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
venv/
.venv/
env/
ENV/

# --- Tooling caches ---
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.tox/

# --- Database (local, regenerable via scripts/seed_db.py) ---
*.db
*.sqlite
*.sqlite3
data/

# --- Evaluation ---
# Harness checks (oracle, offline stub) and pilot runs are regenerable and are NOT
# model results - committing their numbers would invite reading them as such.
# Only eval/reports/*_live_* (real model runs) belong in the repository.
eval/reports/*_oracle_*
eval/reports/*_offline_*
eval/reports/*_limit*

# --- Streamlit ---
.streamlit/secrets.toml

# --- Editors / OS ---
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini
